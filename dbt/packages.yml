---
packages:
  - package: dbt-labs/codegen
    version: 0.14.1
  # Já vinha instalado como dependência do codegen; declarado aqui porque os
  # testes dos modelos usam dbt_utils diretamente (unique_combination_of_columns,
  # expression_is_true, at_least_one).
  - package: dbt-labs/dbt_utils
    version: 1.3.3

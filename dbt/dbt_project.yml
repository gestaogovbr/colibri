name: colibri
version: '0.1.0'
profile: colibri
vars:
  timezone: 'America/Sao_Paulo'

model-paths: ["modelos"]

models:
  colibri:
    staging:
      +materialized: table
      +schema: staging
    intermediate:
      +database: lake
      +materialized: table
      +schema: intermediate
    marts:
      +database: lake
      +materialized: table
      +schema: marts